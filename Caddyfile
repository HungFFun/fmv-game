# Caddy TLS edge: tự xin/gia hạn Let's Encrypt, proxy toàn bộ vào nginx `web`.
# Domain FREE qua DuckDNS (fmv-game.duckdns.org → 45.77.251.153).
# Đổi domain: thay dòng dưới bằng tên mới rồi restart caddy.
fmv-game.duckdns.org {
	encode gzip
	reverse_proxy web:80
}
